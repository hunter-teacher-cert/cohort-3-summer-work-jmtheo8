import java.io.*;
import java.util.*; 

public class arr {
public static void main (String[] args){


// declring the data varibale datat type

  int [] arr = {2,4,6,2,10};

  for(int element:arr){
      // loop through every elemnt in the array. Every time we go through this loop, we will decalre a new 'element' that is equal ':' to the nexy element in the array. Example on the first loop "element" will be equal to 2, on the next loop "element" willl be equal to 4 unitl yiou oop through the entire length of the loop (at index 4) and the loop is broken aka the conditon is false.  
    System.out.println(element);
    
  }
 }
}